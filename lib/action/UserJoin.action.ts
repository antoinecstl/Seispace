// UserJoin.action.ts

export async function handleUserBet(userId: string, betAmount: number) {
    // Ici, vous pouvez ajouter la logique pour traiter le pari de l'utilisateur
    console.log(`User ${userId} has bet ${betAmount} $SEI`);
    // Vous pouvez enregistrer le pari dans une base de données, ou effectuer d'autres actions nécessaires
  }
  